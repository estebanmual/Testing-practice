const capitalize = require('./task4');

it("", () => {
    const answer = capitalize('esteban')
    expect(answer).toBe('Esteban')
})

it("", () => {
    const answer = capitalize('barcelona')
    expect(answer).toBe('Barcelona')
})

it("", () => {
    const answer = capitalize('madrid')
    expect(answer).toBe('Madrid')
})